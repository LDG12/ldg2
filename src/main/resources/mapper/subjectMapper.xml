<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.person456.ldg.dao.SubjectMapper">
    <select id="selectAll" resultType="SubjectDto">
        select * from subject;
    </select>
</mapper>